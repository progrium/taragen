name: Build
on: [push]
jobs:
  build:
    name: Build
    uses: progrium/github-workflows/.github/workflows/golang-cli-build.yml@main
    with:
      bin-name: taragen
      go-version: '1.23.1'
      pre-build-script: |
        echo "Hello, world!"
    secrets: inherit